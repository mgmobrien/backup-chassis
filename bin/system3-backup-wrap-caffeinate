#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
    echo "Usage: system3-backup-wrap-caffeinate COMMAND [ARGS...]" >&2
    exit 2
fi

if command -v caffeinate >/dev/null 2>&1; then
    exec caffeinate -s "$@"
fi

exec "$@"
